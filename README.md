Make changes locally in your React app’s src folder.
Run npm run build to generate the production build folder.
Copy the build folder’s contents into your GitHub Pages repo and push the changes to GitHub.
